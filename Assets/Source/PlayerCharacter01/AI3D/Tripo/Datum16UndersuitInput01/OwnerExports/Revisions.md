# Preserved owner body exports

On 2026-09-17 the owner supplied a replacement at the existing
`tactical+jumpsuit+3d+model.fbx` drop path and authorized a current-mesh assessment.
The controller left that working file unchanged and preserved named inputs:

- `Revision01/`: exact prior bytes recovered from the committed Git LFS object.
- `Revision02/`: exact new owner bytes, selected for Datum16MeshUsability01.

`SourceRevisions01.json` records identity, size, provenance and hashes. The
unversioned drop is owner-controlled; historical intake/MetaHuman evidence and
the old registry entry reference its previous Revision01 bytes. A changed-file
warning for that old path must not be silently rebaselined or corrected by
overwriting the owner's replacement. Named revisions are new inventory paths,
not production or visual acceptance.

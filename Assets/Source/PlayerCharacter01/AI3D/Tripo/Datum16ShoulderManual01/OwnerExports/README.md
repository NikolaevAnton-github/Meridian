# Owner export destination

Save the first untouched Tripo FBX download here, together with any sidecar files
or the original downloaded archive. Suggested basename:
`Datum16-ShoulderR-P2-Quad3000-Try01`.

Do not overwrite an existing attempt. Increment Try02, Try03 only when a later
attempt is actually requested. The controller will inspect and register returned
sources before fitting. No owner output existed when this folder was prepared.

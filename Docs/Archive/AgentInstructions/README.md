# Agent instruction archive

The owner requested compact project instructions on 2026-09-17. Active durable
rules are in [AGENTS.md](../../../AGENTS.md); current scope and decision links are
in [ProjectState.md](../../ProjectState.md). This archive is historical evidence,
not an execution queue or an additional active instruction file.

## Exact pre-compaction snapshot

- File: [2026-09-17-before-compaction.md](2026-09-17-before-compaction.md)
- Original location: repository-root `AGENTS.md`.
- Size: 32,693 bytes; 450 lines.
- SHA-256: `a71842468c314806e625f119c4b4e6eb8fda31167a648177912680c1e4da57a4`.
- Preserved byte-for-byte, including original line endings; no banner was inserted.
- The local `.gitattributes` disables Git newline conversion for these snapshots.
- Paths inside the snapshot are relative to the repository root, as originally written.

Old "current", "latest", approval-pending and dispatch statements in the snapshot
belong to their dated tasks. Later owner decisions can supersede them. In particular,
old lobby backlog directions do not reopen owner-cancelled work, earlier map hashes
do not identify live bytes, and the earlier blanket paid-service exclusion has the
later narrow exception for the owner's existing Tripo/Meshy web allowances.

Existing task reports, approval records and assets remain in place and unchanged.
The snapshot is intentionally not named `AGENTS.md`, so it cannot become a nested
project instruction file. Do not append new status entries to the frozen snapshot.
Local preservation and verification records are under `Saved/AgentInstructions/Compaction01/`.

## ContextBudget01 snapshot, 2026-09-24

[2026-09-24-before-compaction.md](2026-09-24-before-compaction.md) preserves the
13,596-byte root instructions exactly. The companion ProjectState snapshot and
lookup routes are indexed in [ContextHistory](../../ContextHistory.md). Exact
fingerprints are in [ContextBudget.json](../../../Tools/ContextBudget.json).
This snapshot is historical evidence; its relative links retain repository-root
semantics. Do not read it as additional startup instructions.

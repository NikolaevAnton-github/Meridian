# LobbyMaterials-Complete01 / Correction02

**The neutral far field is implemented and technically verified. R1 remains visually unresolved; this is not a completed glass-likeness result or owner-review readiness.** R2's corrected broad-wall response is retained without regression attributable to the support. Independent Review02 must assess the actual final images and this explicit limitation.

Exact deliverable: [identity.json](../Saved/OpeningLobby/MaterialsComplete01/Worker/Correction02/identity.json) and [manifest.json](../Saved/OpeningLobby/MaterialsComplete01/Worker/Correction02/manifest.json). Evidence paths below are relative to `Saved/OpeningLobby/MaterialsComplete01/Worker/Correction02/`.

## Explicit support exception

The current sole map remains `/Game/Maps/L_OpeningLobby_PainterStone01`. One new actor, `MC02_ProvisionalNeutralFarField`, uses `/Engine/BasicShapes/Sphere.Sphere`, centered at (0,0,0) cm with scale (2000,2000,2000), giving a 100,000 cm radius. The nearest shell is hundreds of metres beyond the lobby. It has collision disabled at both actor and component level, casts no shadows, receives no decals, and has dynamic indirect and distance-field lighting influence disabled. It is visible in editor and game.

The new native two-sided unlit sky material is `/Game/OpeningLobby/MaterialsComplete01/Correction02/M_MC02_NeutralFarField`. Its two connected nodes implement a continuous neutral RGB horizon/zenith field: horizon 12, zenith 2, lower hemisphere 3, with broad Y-direction modulation of 18 percent. Smooth transitions reach the upper/lower endpoints at normalized direction Z of +0.70/-0.50. These are material radiance inputs, not calibrated illumination measurements. No sun, cloud, star, scenery or near-pane backing was created. This provisional inspection field establishes no exterior design, narrative fact or final atmosphere.

The exact editable equation and actor settings are in [recipe-Setup01.json](../Assets/Source/OpeningLobby/MaterialsComplete01/Correction02/recipe-Setup01.json), `setup-Setup01.json` and `support-census.json`. The glass itself has no new emission, graph edits or pixels. All 107 prior material bindings and every preexisting scene property remain exact. Support-off/on images intentionally have different environments; they retain identical existing lighting, exposure, renderer and camera settings.

## Two bounded setups and outcomes

1. **Setup01, retained:** sphere only. Four whole/near/entrance/hall images and two pane-hidden controls were inspected. The previously black entrance leaves become visible; the far-field gradient is clearly visible with panes hidden. The final lower panes transmit it, but the large fixed panes retain strong, comparatively uniform grey surface response. No clipped white pane samples were found. Fine frames remain legible, with etched microresponse in the near-oblique image. This improves readability but does not establish convincing fixed-glass likeness or adequate fixed/leaf distinction.
2. **Setup02, rejected and removed:** one map-local `SphereReflectionCapture` at (-2500,0,950) cm, radius 2300 cm, brightness .7, captured-scene source, zero runtime skylight leakage. The purpose was to supply reflected scene structure missing in Setup01's near-oblique fixed panes. The selected runtime implementation is skipped by the installed renderer's unchanged `r.ReflectionCapture.Runtime=0`. The native source was inspected after the trial exposed this limitation. Its images therefore **do not test a working reflection contribution**, and cannot show that local reflection support in general is ineffective. No renderer setting was changed. The exact rejected actor properties, map bytes and five images remain under `Setup02/`; only this task-created actor was removed. No Skylight was added. The smallest implemented support was retained; no third setup was attempted.

Setup01 native material and map bytes before the second trial are archived under `Setup01/NativeArchive/`. Both setups retain exact settings and evidence. The worker did not reuse old failed lightstudy assets or results.

### R1: partial improvement, unresolved

`Final/entrance-whole-90.png`, `entrance-90.png` and `entrance-glass-90.png` show readable entry leaves and a brighter neutral lower field. However, the tall fixed panes still resemble a smooth grey sheet at the whole-entrance pose. Their surface response masks most of the stronger gradient seen in `Hidden/`. This is **not** sufficient R1 closure. The remaining problem is observed appearance, not missing final screenshots. The unsupported optional reflection trial also leaves the effectiveness of properly functioning local reflection support unverified.

Two true/hidden/opaque triplets use identical interior standing poses and the same sphere state. Opaque accepted stone on the same six meshes blocks the far field; hiding the panes reveals it; the final optical panes transmit it. Four matched support-off/on pairs isolate the sphere's effect. `transmission-verification.json` records named unedited-image sample regions, nonclipping checks and their limited interpretation. The verified unchanged native glass graphs and prior reverse-side controls remain linked through Correction01. These establish genuine transmission, not visual acceptance or calibrated refraction.

### R2: corrected response retained, independent recheck pending

The 19 corrected broad-stone assignments and all their material/source bytes remain exact. Final side-aisle, bays-context and terminal-wall/soffit images retain the prior broad-field mineral response. The terminal wall has no dominant white disc. Side walls retain a broad oblique sheen; protected accepted samples and untouched structural elements still show bright highlights, as before. The sphere produced no observed broad-wall, ceiling, floor, checkpoint or elevator palette regression. This report does not claim the hall is free of existing highlights.

## Verification and preservation

- 127 preexisting actors and 148 components compare exactly before/after/reopen/restoration, including all world, collision, gameplay, lights, exposure and renderer properties. The comparison removes only the explicitly inventoried new sphere actor. Final totals are 128 actors, 149 components and 108 mesh components; the extra sphere is separately counted, leaving **107 lobby components** fully covered.
- All 107 effective bindings and override arrays are unchanged: 44 stone, three floor/strips, 45 metal, nine ceiling and six glass. All 17 accepted bindings, nine ceilings, exact shoulder assignment, blank opaque field and owner checkpoint geometry remain preserved. The inventory's temporary Python address strings in location/bounds are not binding evidence; the complete reflected-property comparison independently verifies geometry without normalization.
- The support material and map were saved and reloaded. Native graph traversal confirms two connected nodes, compiled shader statistics, no texture/Game dependencies, exact recipe code and the expected unlit/two-sided/sky settings. Existing material source and regeneration receipts are carried forward with verified hashes; no ceremonial DCC source was created.
- The controller's exact pre-support map/manifest/identity archive verifies. Five historical manifests verify 1,187 entries using exact archived map bytes; no old manifest is rebaselined. Protected sources, assets, configuration, helpers and historical evidence verify, with one externally changed live controller transcript explicitly recorded in `protected-after.json`. The worker never wrote that transcript and did not overwrite the controller's newer data.
- 29 native 1920x1080 HFOV90 images include 12 final standing views, four support-off/on pairs, two same-state true/hidden/opaque triplets and retained trial images. Camera poses, actual FOV, runtime possession/standing/ticks, exposure and renderer metadata verify. All twelve final images and controls were visually inspected against both actual owner reference PNGs. No image edits or cosmetic crops were used.
- Diagnostics and capture settings were restored. PIE is stopped; the clean current map is loaded. Prior positive movement/collision evidence is reused only through exact property preservation; no new route or performance claim is made.

Measured additional lobby growth is about **69 MB**, combined batch growth **283 MB**, cumulative lobby storage **2.129 GB**, and project storage **13.581 GB**, within the requested targets and caps. Exact final measurements are in `storage.json`. No user/history assets were deleted.

## Handoff

Return `LobbyMaterials-Complete01/Correction02` to the controller with **R1 unresolved**, **R2 retained pending independent recheck**, and **owner_review_ready=false**. The requested full visual finish has not been achieved. The renderer constraint of the rejected runtime reflection trial must not be generalized into proof that every authorized support route is ineffective. No task administration, comments, delegation, later dispatch, registry mutation, configuration/skill changes, install, paid service, commit or push was performed. All editor production/capture actions used the official Epic MCP; Rider was used once to register the bounded task toolset.

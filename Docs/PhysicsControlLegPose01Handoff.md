# MSQ-91 owner-test handoff

Candidate07 corrects the reproduced post-step knee reversal and accumulating
stance error on MSQ-89 Candidate05. Execution follows the owner's
[start and independent-review waiver](Approvals/PhysicsControlLegPose01-OwnerStart01.json).
The [executor report](PhysicsControlLegPose01.md) records implementation, bounds,
measurements, before/after recordings and limits. Final motion/play acceptance
remains with the owner; no independent technical or visual verdict is claimed.

One existing Multica Unreal executor performed implementation and bounded
corrections in run `01a0c03b-ec4a-73e6-bdef-45767d4a8597`. Saved profile, actual
native process arguments and session context confirm Astra/max/default with
fast mode disabled. No profile change was required. The controller accepts scope,
candidate identity, evidence applicability and preservation without repeating
the executor's gameplay self-checks.

All 34 Candidate07 manifest entries match the current and frozen files. The
executor validation establishes identical native/asset bytes for all 14 entries
shared with recorded Candidate06. Controller acceptance is recorded in
`Saved/CombatSlice01/PhysicsControlLegPose01/Controller/handoff-acceptance01.json`.

The reproduced baseline reached about 120.1 degrees of geometric knee-to-toe
direction mismatch. Final settled samples remain within 13.65 degrees, close to
the retained idle pose. Neutral leg frames and pelvis height no longer accumulate
the solved pose across episodes. Excessive stance width requests the remaining
corrective step within the two-step budget; infeasible targets retain physical
collapse and living get-up. These measurements are engineering diagnostics for
this mannequin, not clinical anatomical limits.

The final Development Editor build and 183 focused assertions pass across 2,425
runtime samples and 22 CPU skin audits. Evidence covers comparable rear/side
views, left/right and four repeated episodes, ordinary and turned lateral
corrections, a 40 cm corrective case, infeasible-target fall/get-up and reset
during/after movement. Peak support-foot drift is 0.415 cm; measured sole gaps
are 0.225-0.550 cm against the retained 1 cm tolerance. Earlier failures remain
preserved and their bounded corrections are explained in the executor report.

Candidate06 supplies the affected corrective-step rechecks. Applicable Candidate05
records are reused for branches unchanged by the final numeric comparison and
pending-correction state fix. Candidate07 freezes the same native/asset bytes as
Candidate06 plus final scripts and documentation. Unaffected MSQ-89 rendering,
controls and input/clock evidence retains its original identity; its old leg-pose
trajectories do not establish acceptance of the new pose.

All 10 executor preservation checks pass. Owner engine/project edits, lobby,
original Mixamo sources, retained Physics Asset and recovery graph remain intact.
No binary asset changed and no new asset registration is required. Generated
records remain under `Saved/CombatSlice01/PhysicsControlLegPose01/`.
The controller owns the task-scoped local closure commit and Multica status.

The executor handoff records the retained lobby, PIE stopped, no dirty packages
and no transient probes. The editor writer lease is released for owner Play.
This remains assisted recovery on flat static floors with the existing rifle
idle/get-up assets. Residual idle knee bend, retargeted shoulders, compliant joint
overshoot and roughly 16-17 FPS recording capture remain documented limits.
MSQ-92 and all other successors remain undispatched.

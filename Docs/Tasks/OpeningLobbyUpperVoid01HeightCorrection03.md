# HC-R1: diagnose attenuation stages and extend the visible transition

This is a bounded continuation of the owner's authorized correction and the
required independent-review finding HC-R1. No new owner decision is inferred.
Height placement and side-aisle readability are already correct. Root inspected
HeightCorrection02/Worker/Trial02/close-column-90.png and still observes the same
apparent paired terminal edge, dimmer than before. The two allowed Correction02
trials therefore do not close HC-R1. Preserve their complete bytes and handoff.

Change the diagnostic approach instead of repeating darkness/intensity tweaks.
Read `HeightCorrection03/Controller/diagnostic-brief.md`, the owner yellow image,
and the original HC-R1 report in HeightCorrection01/Review01. A read-only analysis
found the new exponential curve compresses the 90–10% postprocess transition
to 1.49 m, with 90% of transmission lost by 15.6 m. Calling its very dark remainder
a long tail did not extend the visible disappearance. Reducing central lights
made the same short transition dimmer. The whole available high zone projects
to roughly 35 pixels at the required close pose; inspect native pixels directly.

## Bounded different approach

First isolate light-function attenuation versus postprocess attenuation at the
exact failed close pose. Preserve each tiny diagnostic's actual settings/frame.
Use these observations to assign one job to each stage: a broad approximately
linear visible descent across nearly the whole owner-marked high zone, with
rounded endpoints; roof illumination suppression separately. Remove aggressive
exponential compression and avoid multiplying two concentrated ramps. Keep onset
approximately 13.7–14 m and the main 18 m roof fully concealed; side ceilings at
9.2 m must remain readable. Do not merely translate a short ramp.

Do not retain Correction02's reduced central illumination if it offers no visual
benefit. You may restore its six central fills toward HeightCorrection01's exact
values, or tune only those six with evidenced benefit. Keep all twelve aisle
fills exact. Existing surface material/source/107 bindings, geometry, owner edits,
glass/support, gameplay, fixed exposure and configuration remain protected.

At most two representative different setups after the small isolation comparison.
If the full useful surface-height range still gives the same cap cue, test one
world-anchored upper-volume absorption approach allowed by the original task,
with smooth density onset above the lower enclosure. Dedicated native atmosphere
graphs/technical volumes are allowed; new visible geometry or architectural
changes are not. Do not pursue endless scalar retries. Report concrete remaining
limitation and retain best evidenced state if neither approach succeeds.

## Efficient evidence and identity

- Multica MSQ-31, Environment Artist, Astra/high/standard, one editor writer.
- Candidate `LobbyAtmosphere-UpperVoid01/HeightCorrection03`.
- New work `Saved/OpeningLobby/UpperVoid01/HeightCorrection03/Worker/`.
- New source `Assets/Source/OpeningLobby/UpperVoid01/HeightCorrection03/`.
- New helpers `Scripts/OpeningLobby/uppervoid01_volume_*.py`.
- Author report `Docs/OpeningLobbyUpperVoid01HeightCorrection03.md`.
- Controller archive `HeightCorrection03/Controller/BeforeCorrection/archive.json`.

Verify live clean project/map/PIE and exact archive before mutation. Reuse existing
pure primitives and capture/input/graph/property adapters with new outputs. Do
not reread/rebuild the entire validation framework. Carry forward proven unchanged
gameplay/material/history checks and audit changed deltas, hashes and archive
chain. Do not alter old helpers, source, reports, failed trials or manifests.

Use a compact failed-pose diagnostic at actual 1920x1082, followed by native
compact nearby/context/aisle/reflection regression evidence. A short real-input
approach/turn and >=5 s upward hold is required, referencing prior full-route
coverage for unchanged gameplay. Save/reopen and verify intended properties,
native graphs/source and all 107 bindings. Restore temporary controls; PIE off,
map clean. One bounded performance sample if the graph cost changes substantially.
Retain honest metadata and sample limits. Freeze exact manifest for fresh MSQ-32
independent recheck after the final useful candidate, not each failed diagnostic.

Aim <=15 MB incremental evidence/source/rollback, with explicit total accounting.
The original 100 MB task-growth and 2.4 GB lobby values are planning targets;
the required extra diagnostic may exceed them modestly and must report that
variance, never erase history or claim those numerical checks pass. Hard project
cap remains 250 GB. This bounded continuation is authorized to resolve HC-R1,
not permission for unrelated generation or indefinite growth.

No delegation, Multica administration/comments, controller/approval/instruction/
configuration edits, installs, paid API, registry, commits/pushes or later dispatch.
Official Epic MCP; the previously authorized minimal Rider helper registration
is allowed if needed. English project content. No owner acceptance or glass R1
closure. Controller remains through the independent review and bounded correction.

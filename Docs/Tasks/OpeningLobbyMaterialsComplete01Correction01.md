# LobbyMaterials-Complete01 / Correction01

The owner requested all remaining lobby materials together, with no intermediate
family approval gates. MSQ-28 completed the first complete batch; fresh MSQ-29
Review01 confirms technical coverage/preservation but identifies visual failures.
Execute the required bounded corrections in Review01/verdict.json and report.md.
Candidate `LobbyMaterials-Complete01/Correction01`. Multica Environment Artist,
Astra/high/standard, existing subscription/project, concurrency one. English.

## Read and preserve

Read Docs/Tasks/OpeningLobbyMaterialsComplete01.md, this correction task,
Saved/OpeningLobby/MaterialsComplete01/Review01/first-look.md, report.md and
verdict.json. Inspect both actual OwnerReferences01 PNGs and the initial actual
Final entrance-whole-90, entrance-glass-90, entrance-90, side-aisle-90,
bays-context-90, stone-oblique-90 and context images. No new family approval pause.

Current sole map: /Game/Maps/L_OpeningLobby_PainterStone01. Initial manifest:
Saved/OpeningLobby/MaterialsComplete01/Worker/manifest.json, SHA-256
4034e0be05cd472593fd4101691d4f2b7b03baacf5830518938606aec134daf5.
Initial map SHA-256:
57dd3a44c1e5e238b14fd7054cd23d05f8411787b91c506711ca466226680d10.
The controller archives its exact bytes before correction; verify the archive
receipt under Controller/BeforeCorrection01/archive.json before editing.

Verify live project/map/PIE/dirty state and exact bytes before editing. Preserve
any later owner edits or unsaved DCC work. No loading old maps over current work.
One writer owns the full editor operation. All 17 original accepted bindings
and accepted Stone01/Floor01/Metal01 source/texture/material bytes stay exact.
Preserve all ceiling source/assets/bindings from the first batch, all earlier
history, and all nonmaterial scene/actor/component properties including geometry,
slot layout, mesh defaults, owner checkpoint X -2090 cm, lighting/exposure,
collision and gameplay. The logo field stays blank opaque stone.

Preserve initial Worker files, initial native graphs, helpers and author report
unchanged. Use new correction graph assets under
/Game/OpeningLobby/MaterialsComplete01/Correction01/ and new focused helpers
Scripts/OpeningLobby/materialscomplete01_correction*.py. New evidence goes only
under Saved/OpeningLobby/MaterialsComplete01/Worker/Correction01/. New editable
recipes under Assets/Source/OpeningLobby/MaterialsComplete01/Correction01/.
Write Docs/OpeningLobbyMaterialsComplete01Correction01.md. Initial review and
initial manifests remain immutable, resolved via exact map archive as needed.

## Bounded corrections

1. Improve the six glass panes' actual interior appearance and entrance
   readability. Current reverse-side transmission is technically proven, but
   presentation views show grey/purple sheets, broad white glare and nearly black
   entry leaves. Diagnose actual native optical parameters and interaction with
   existing geometry before choosing a correction. Author separate new native UE
   fixed/leaf graph variants to make the pane bodies read as believable diffusing
   architectural glazing and the entry leaves recognizable from inside. Preserve
   real transmission/reflection and physically coherent dielectric behavior.
   Do not substitute opaque/emissive surfaces, arbitrary colored patches,
   painted reflections, outlines, reference pixels, added lights or backing.
   Judge whole entrance and near oblique interior views, not reverse-side
   diagnostics alone. A neutral RGB edit or shader parameter change is not a
   visual pass by itself. Record what actually improves and any remaining
   constraint. Use at most two purposeful optical candidates; after equivalent
   failures change the diagnostic approach instead of repeating blind tweaks.

2. Quiet the excessive broad highlights on the newly rolled-out stone/wall
   surfaces, especially side-aisle and bays-context, while preserving the accepted
   stone's mineral identity and spatial hierarchy. Author new UE response variants
   referencing accepted Painter stone textures read-only, with justified roughness/
   specular calibration. Do not alter accepted source assets, their four sample
   assignments, any floor/metal, ceiling, lighting or exposure. Target explicit
   newly assigned broad side-wall/room-shell fields only (a justified subset of
   the 40 rollout slots), expanding only to newly assigned adjacent overhead stone
   if the same correction requires it,
   retaining colour, mineral scale and continuity. The protected accepted shoulder
   pair must retain its exact shared accepted assignment.
   Do not flatten the whole hall, crush reflectance, change BaseColor to disguise
   blown highlights or invent seams. Compare with original reference and initial
   completed batch. At most two purposeful stone response candidates.

Only component material overrides among six glass and forty newly rolled-out
stone/wall slots may change. A new coverage plan must name exact targets and
justify any subset. All 107 visible components remain fully material-covered;
no proxy/default/error bindings. New UE graphs/recipes are editable source;
reuse accepted native Painter content without ceremonial new .spp creation.
If genuinely new opaque pixels are necessary, author them natively in Painter
with new correction .spp/exports and final save/reopen/regeneration proof; do
not alter the accepted or first-batch sources. Avoid new texture work unless
the identified failures require it. Native advanced Painter tools remain scoped.

## Verification and handoff

Before mutation save correction-response plan, exact full scene properties and
protected-file hashes. Reuse existing validators with inspected new output paths;
never call initial hardcoded mutations/exports or write their evidence. Compare
before/after properties permitting only the named correction overrides. Verify
original 17 bindings, all ceiling bytes/bindings, previous full 107 coverage and
all accepted/history bytes. Reopen/reload saved new UE packages and scene, audit
compiled dependencies and actual node links; no old rejected pixels/assets.

Capture same-pose 1920x1080 HFOV90 final correction views for entrance-whole,
entrance-glass, entrance, side-aisle, bays-context, stone-oblique, whole hall and
at least one ceiling/metal/floor context regression view. Standing eye height,
actual pose/FOV/tick/exposure/renderer sidecars. Keep diagnostic controls separate.
Recheck changed glass transmission with meaningful existing control images or
fresh bounded controls if changed optics invalidate the prior evidence. Inspect
actual images before claiming either correction resolved. No cosmetic crops or
processed screenshots as substitutes. Use passing prior route checks narrowly
with exact collision/gameplay preservation; do not repeat the full walk suite.
Restore all capture/diagnostic state; leave current clean map loaded, PIE off.

Deliver Correction01/identity.json and unique nonempty path/bytes/SHA-256 manifest
binding exact corrected map, new graphs/source/helpers, changed evidence and
unchanged source/history links. Include full final coverage, exact initial map
archive and a response to each Review01 required correction. Source .spp identity
can reference the unchanged first-batch Ceiling and accepted source receipts;
do not call their old regeneration tools merely to repeat already passing work.
State visual limitations honestly. At most 150 MB additional correction growth,
within <=400 MB total batch growth, 2.4 GB cumulative lobby planning limit and
hard250 GB project cap. No user/history deletions, installs/downloads, paid
service, config/skill changes, task comments/admin, delegation, registry,
commits/pushes or later dispatch. Controller sends fresh focused Review02.

# UpperVoid01: one spatially varying extinction experiment

The owner requested darkness only in the main upper hall, near the yellow section
band, with the lower side passage ceilings visible. Those placement requirements
are met. HC-R1 remains open: at the exact close upward pose, a horizontally
uniform extinction region still makes the two central shafts appear to end
together. HeightCorrection03 tested isolated stages, broad near-linear display
transmission and ray-integrated height-only absorption. Preserve every result.

This is one final bounded, materially different experiment within the authorized
atmosphere correction. The controller asked an optional preference about a slightly
irregular boundary; no answer was received. Mild spatial irregularity is the stated
working assumption, not owner approval of an exact new visual design. No additional
architecture, geometry or visible dressing is authorized.

## One experiment, not another scalar series

Before the representative field, follow fresh Review02's specific diagnostic:
at the exact close pose capture one reference with BOTH atmosphere stages disabled,
then compact temporary outputs showing actual rendered surface world Z and evaluated
PP transmission across the apparent terminal band. Establish whether the apparent
edge corresponds to shaft geometry, source lighting, or the displayed attenuation.
Restore diagnostic outputs after capture. This is not satisfied by the previous
one-stage-at-a-time isolation. If it reveals an implementation/sampling defect,
correct that specific defect as the single representative method rather than
adding spatial variation on top of incorrect inputs. Preserve diagnostic evidence.

Use a static, smooth three-dimensional extinction field in the existing dedicated
native atmosphere graph. Vary absorption across each shaft width and between shafts
with broad, low-frequency features approximately 2-5 m across. Local onset may vary
continuously within 13.7-15.1 m; absorption must remain zero below 13.7 m. Preserve
guaranteed main-roof concealment before 18 m. Avoid a constant height offset per
column, which would simply stagger apparent caps, and avoid visible stripes,
small noisy speckle, moving fog or obvious cloud patches.

Integrate the spatial field along the camera-to-surface ray if required. Keep all
current lights fixed, including the twelve aisle fills. Only dedicated atmosphere
graph/source changes and any strictly necessary native atmosphere parameter changes
are allowed. Preserve all 107 surface assignments and original source/material
bytes, owner geometry, glass/support, collision/gameplay, fixed exposure and config.

Inspect actual native pixels at the exact failed pose and a nearby lateral pose,
plus one short moving approach/upward look and >=5 s hold. Retain compact aisle,
context and reflection regression evidence. One representative field only; fix
implementation errors if found, but do not tune a series of aesthetic variants.
If it makes wavy caps, patches, lower haze, or no clear improvement, explicitly
report failure. Retain the safer evidenced state, restoring the previous exact
assets if the trial regresses the current view. Either disposition must have an
exact final identity and clean editor. Do not force an HC-R1 pass.

## Execution and evidence

- Multica MSQ-31, Astra/high/standard, subscription authentication, one editor writer.
- Candidate `LobbyAtmosphere-UpperVoid01/HeightCorrection04`.
- Output `Saved/OpeningLobby/UpperVoid01/HeightCorrection04/Worker/`.
- New source `Assets/Source/OpeningLobby/UpperVoid01/HeightCorrection04/`.
- New helpers `Scripts/OpeningLobby/uppervoid01_spatial_*.py`.
- Report `Docs/OpeningLobbyUpperVoid01HeightCorrection04.md`.
- Exact rollback `HeightCorrection04/Controller/BeforeCorrection/archive.json`.

Read the prior fresh Review02 before work. Verify correct live project/map, PIE off,
clean state and archive before mutation. Use official Epic MCP; the existing minimal
Rider helper-registration route is allowed if needed. Reuse inspected helpers and
validators; no new framework. Carry forward immutable passing unchanged evidence,
verify changed graph/source, deltas, archives and 107 bindings, save/reopen, check
completed graph compilation, restore capture/input state and leave PIE off/clean.
One bounded performance observation is sufficient if graph complexity increases;
record actual pose and limits honestly, without unsupported matched-view claims.

Aim <=15 MB incremental evidence/source/rollback. The combined task100MB and
lobby2.4GB planning values are already exceeded; report totals and new growth
honestly. Modest necessary evidence growth is allowed; the hard250GB project cap
remains. Preserve history; no unrelated generation or indefinite work.

No delegation, task administration/comments, controller/approval/instruction/config
edits, installs, paid APIs, registry, commits/pushes or later dispatch. English
project content. The controller will obtain fresh independent bounded recheck and
then hand off either the verified correction or this specific evidenced visual
limit for owner direction. No owner acceptance or deferred glass closure.

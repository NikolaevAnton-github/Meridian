# MSQ-54: Datum16MetaHumanTrial01

Completed 2026-09-17: [worker handoff](../PlayerCharacter01MetaHumanTrial01.md)
and [controller review](../PlayerCharacter01MetaHumanTrial01Review.md).
Only this bounded experiment is closed; full MSQ-54 remains incomplete.

The owner authorized this next protagonist experiment on 2026-09-17 with
Astra/max/standard. Read the [exact instruction](../Approvals/PlayerCharacter01-MetaHumanTrial01.json).
This supersedes previous research-only dispatch restrictions for this bounded
trial. Datum16 is selected and MSQ52-RigContract01 is complete. The existing
MSQ-54 issue remains the production task; do not create another task family.

## Purpose and inputs

Determine whether UE 5.8 From Custom Mesh can supply useful fitted geometry,
topology and rigging for the original returned coverall before extensive manual
retopology. A measured unsuccessful trial is a valid result if it identifies
the actual failure and next repair route. Installation evidence alone is not
completion; attempt the live workflow and pursue bounded justified corrections.

Read Docs/ProjectState.md, Docs/Research/PlayerCharacter01AI3D/MetaHumanVideoStudy01.md,
Docs/PlayerCharacter01BodyIntake01.md, Docs/PlayerAnimationAudit01.md, the task
plan/index and AI3D pipeline. Use current local capabilities and Epic primary
documentation; avoid repeating the full video research.

Untouched source:
`Assets/Source/PlayerCharacter01/AI3D/Tripo/Datum16UndersuitInput01/OwnerExports/tactical+jumpsuit+3d+model.fbx`.
SHA-256: `fc6340b6f36c529f15dac48ef06dca37aa72a07134b873e9e564f0addc6fa196`.
The generated T-pose, declared scale and featureless hood are not approved
production proportions or a replacement for the selected helmet.

## Execution and acceptance

1. Record source and rig-contract hashes, current dirty files/packages, live
   editor/project identity, installed capabilities and initial storage/memory.
   Preserve the pre-existing DefaultEngine.ini edit. One Unreal/Blender writer,
   one heavy workload and one Multica production run. Existing apps/services may
   be launched. Prefer official Epic MCP; reuse the existing transport and
   animation-audit tools. Inspect actual available APIs before assuming features.
2. Create a named editable derivative under
   `Assets/Source/PlayerCharacter01/MetaHumanTrial01/`. Record units, axes,
   transforms and temporary experimental scale against the measured rig and
   fixed 170 cm camera / 34/88 cm capsule / 90 degree FOV. Do not silently change
   gameplay scale. Correct only solve-blocking defects, with retained before/after
   geometry and rationale. Blender tool choice is discretionary.
3. Enable installed Creator prerequisites in this project only as necessary,
   preserving unrelated settings and dirty work through any restart. Prove actual
   module loading and run From Custom Mesh. Prefer body-only fitting if the hood
   obstructs facial correspondence; do not invent an exposed face design. Use the
   smallest useful local trial. Existing included Epic processing is within this
   experiment; no new purchase, paid API, provider generation or public upload.
   Report an unavoidable interactive account/license gate precisely if encountered.
4. Inspect Auto Solve and all ten finger/thumb correspondences from multiple
   sides. Correct identified mapping errors with manual landmarks where supported.
   After two equivalent failures change the diagnostic approach. Retain failures;
   no broad unrelated engine/tooling project. Save source-pose DNA separately from
   final assembled A-pose outputs and controlled exports, when available. Verify
   the actual generated geometry, skeleton, UVs and weights rather than inferring
   success from a save operation.
5. Compare source and fitted result in neutral gray front/back/side/oblique views
   and hand/joint closeups. Record silhouette/volume loss, wrists/cuffs, crotch,
   ankles/boots and garment detail. Decide whether it is useful as the visible
   garment surface or as a fitted base for retaining the original garment.
6. If a usable fit is obtained, perform bounded deformation and compatibility
   probes on derived assets: arm raise/cross-body reach, elbow/forearm twist,
   rifle grip and isolated trigger finger, four ordinary/empty aimed/unaimed
   reload cases with synchronized rifle/magazine motion, deep crouch and ankle
   flex. Preserve additive bases and actual source timing. Reuse the MSQ-52
   playback helpers. Distinguish playback on an unchanged reference rig from
   deformation of the new surface. If a preceding fit/export failure makes a
   probe impossible, mark it unverified with the causal evidence; do not fake a
   pass or perform an extensive unrelated retarget implementation.
7. Compare keeping the exact existing 161-bone production contract with a candidate
   MetaHuman integration route. Do not replace/rebaseline the accepted contract.
   State the evidence, remaining repair, effort and risks for the recommended
   route. Finish a manifest after output files stop changing; measure storage
   growth and memory, keeping the entire project below 250 GB.

## Output and handoff

- Editable trial source and controlled inputs/exports/settings/provenance under
  `Assets/Source/PlayerCharacter01/MetaHumanTrial01/`; binary sources use Git LFS.
- Named experimental Unreal packages under `/Game/Development/PlayerCharacter01/MetaHumanTrial01`;
  disclose unavoidable generated dependencies elsewhere. No lobby map edits.
- Thin task scripts under `Scripts/PlayerCharacter01/metahuman_trial01_*` if needed;
  reuse existing transport/validators instead of a new harness.
- Full diagnostic evidence, logs, images and failed attempts under
  `Saved/PlayerCharacter01/MetaHumanTrial01/Worker/`.
- Concise English handoff `Docs/PlayerCharacter01MetaHumanTrial01.md`: actual
  operations, results, artifact identities, comparison images, measured costs,
  preservation and explicit pass/fail/unverified probes with recommended next work.

The worker does not change task states, comments, profiles, controller-owned
approval/task/state records, registry acceptance, commits or successors. The
controller reviews outputs, requests bounded corrections if needed, restores
the saved profile/native arguments and commits verified task-scoped changes.
This closes only the experiment. Full MSQ-54 still needs coherent production
references, fitted original body/armor/hands and its validated round trip.
Markerless capture, final materials and gameplay implementation are later work.

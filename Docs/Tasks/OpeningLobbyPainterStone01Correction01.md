# LobbyPainter-Stone01 / Correction01

Execute one bounded correction of MSQ-23 Review01's demonstrated visual failures
R1/R2, through MSQ-22 at Astra/max/standard, subscription only, concurrency one.
Read Docs/Tasks/OpeningLobbyPainterStone01.md for unchanged authority and limits,
then Saved/OpeningLobby/PainterStone01/Review01/report.md and verdict.json. This
supplement supersedes the first worker handoff phase only. The controller handles
independent recheck and the owner material-direction gate afterward.

Inspect the actual two owner reference PNGs and current near, return, grazing,
context and complete-portal images. The reviewer passed polish and all technical
criteria, but failed convincing stone likeness, mineral structure and repetition.
Dark rounded/ring-shaped motifs dominate an almost uniformly mottled field and
repeat conspicuously up the 18 m jamb. A tint, seed, sharpening or blur change
alone does not close the findings.

R1: Recompose native Painter color/mineral layers into a quieter green-grey ground,
irregular interrelated mineral clusters, fine texture and sparse pale structure.
R2: Suppress signature motifs and their obvious recurrence on the existing tall
assembly, adding controlled native spatial/material sampling variation only if
needed. Preserve appropriate grain scale, front/return coherence and credible
polish. No new geometry, seams/panel grid, damage, light/exposure changes, or other
material family. All four current bindings and the owner map/edits are fixed.

The read-only helper's potential-correction-notes.md under Controller identifies
layer 177, grunge_stone_details BaseColor Overlay at opacity 0.72/contrast 0.14,
as a likely contributor, not a proven cause. A brief isolated-layer diagnostic
can establish its contribution. Lowering its weight/contrast may help expose a
quieter ground; replace or recompose that native procedural contribution if the
ring-shaped motif remains dominant. Choose the visual result, not prescribed
numbers. Keep the calibrated independent roughness/metallic/height layers unless
actual evidence warrants a change. When editing only color, require identical ORM
and normal exports. Avoid another GUI capture investigation; the prior preview
limitation is documented and native source regeneration was independently proven.

Use genuine native Painter tools and editable .spp authoring. Advanced Fill-layer
resource tools remain configured task-locally. Discover actual parameters and
respect the documented FillLayer/effect limitations. No bridge/config changes,
external texture generation, downloads, paid service, unchanged smart material,
or reuse of rejected NumPy/older lobby textures. Keep the current 2048/240 cm
coverage unless a demonstrated repetition/scale reason justifies a bounded
change without losing fine mineral detail. One purposeful correction, with at
most one adjustment after inspecting its near/context trial, then exact handoff.

The controller has archived every first-candidate live source/native/script/doc
file that this correction may overwrite, plus its manifest/identity, under
Saved/OpeningLobby/PainterStone01/Controller/BeforeCorrection01/. Verify
archive.json before mutation. Review01 and all existing Worker evidence and
exports are immutable. Their archived source/native mappings preserve the exact
reviewed first-candidate bytes. Never restore the older FunctionalBuild worker
map or overwrite the owner-edited FunctionalBuild01 source.

Allowed live asset writes remain Assets/Source/OpeningLobby/PainterStone01/,
Content/OpeningLobby/PainterStone01/, and the current sample map
Content/Maps/L_OpeningLobby_PainterStone01.umap. Update the existing task helper
scripts only under Scripts/OpeningLobby/painterstone01_*.py as needed; do not build
a replacement capture/validation framework. The original scripts are archived.
Current source README, recipe, channels and Docs/OpeningLobbyPainterStone01.md
may be updated to the corrected version, with the prior version archived.

Write all NEW correction records, captures and handoff files under
Saved/OpeningLobby/PainterStone01/Worker/Correction01/. Native Painter exports
must be under Worker/Exports/Correction01/ (within the existing configured root).
Never overwrite the initial Worker's Before/Final/FinalContext/Painter records,
root audits, manifests or prior export directories. Reuse the unchanged source
Before/BeforeContext images and baseline scene data; do not recapture them merely
to rename evidence. Route helper output explicitly to the correction directory
before calling any write operation. The current sample's material can be updated
in place because its initial bytes are archived; all historical source maps and
rejected asset namespaces remain read-only.

Confirm actual project, clean state and PIE before Unreal operations. Use the
official Epic MCP. Re-export the saved/reopened corrected Painter source, import
to the same scoped native sample and check affected dependencies/channels. Capture
the same five Final views and the portal-complete-90 view at unchanged actual
poses, HFOV90, 1920x1080 and source lighting. Confirm world ticks and real camera
readiness, save/reopen native state, compare nonmaterial properties and the four
bindings, check source/history hashes and standing possession, then restore
temporary capture settings and leave the clean current sample loaded, PIE off.
No full route rerun; reuse passing validation narrowly and check changed outputs.

Keep one DCC writer/heavy workload. Initial task artifacts were about 289 MB and
cumulative lobby about 1.357 GB. Measure the bounded archive plus correction
growth; the 300 MB first-pass figure was an aim, while the 2 GB cumulative lobby
and 250 GB project caps remain mandatory. No deleting user assets or prior
evidence to meet a target. No comments/task administration, delegation, registry,
commits/pushes, installs, paid services or subsequent dispatch by the worker.

Hand off candidate LobbyPainter-Stone01/Correction01 with a standard nonempty
unique relative-path/bytes/SHA-256 manifest at Worker/Correction01/manifest.json
and Worker/Correction01/identity.json binding its manifest, map, .spp and source
map. Inventory all current source/native assets, helper/doc sources, relevant new
evidence and unchanged source evidence used by this correction. Do not include
the controller archive as if it were current material input. Include explicit
R1/R2 response and known limitations. This requires a focused independent recheck
and owner direction decision; no full-lobby or atmosphere acceptance is granted.
